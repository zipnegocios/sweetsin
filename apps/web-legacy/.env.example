PORT=3000
BASE_PATH=/
# Google Maps JavaScript API key for the FindUs section map. Restrict it by
# HTTP referrer (your domains + localhost) in Google Cloud Console — it is
# shipped to the browser like any client-side Maps key.
VITE_GOOGLE_MAPS_API_KEY=
